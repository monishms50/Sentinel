Entropy Agent that runs on nodes to collect baselines and detect drift (written in Go).

Dockerfile — Builds the agent container image

go.mod — Go module dependencies

main.go — Agent entry point and lifecycle management