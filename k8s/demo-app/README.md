Sample applications used to generate normal and malicious runtime behavior.

namespace.yaml — Namespace isolation for demo workloads

nginx-deployment.yaml — Web server workload to monitor

postgres-deployment.yaml — Database workload to test sensitive drift